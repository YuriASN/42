INCLUDE

#include //tem de colocar o # antes para mostrar que eh algo pre-processamento
#include <> //<> inclui arquivos do caminho do ambiente de sistema do stdio.h.
#include "" //"" inclui arquivos na pasta do proprio arquivo c

file .h  e' a lista (lib) das suas funcoes.
	Dentro da lib eu coloco a primeira linha da(s) funcao(oes).
	isso faz com que a compilacao confira se os argumentos enviados sao compativeis com argumentos que a funcao recebe.
A declaracao de uma lib deve corresponder a funcao .c existente.

Para proteger as libs de loops (deve-se sempre fazer), e' necessario criar um IFNDEF com
	o nome da lib modificado (__FT_nomedalib_H__) e dentro dele definir o mesmo ifndef.
	assim depois de entrar no indef uma vez ele nao voltara a mesma lib pois o IFNDEF foi definido.



DEFINE

Define uma palavra e seu substituto na compilacao
#define LENGHT 42	//LENGHT = 42
#define LOLO(x) int	x; //Nesse caso o x sera substituido por o que esteja entre parenteses
						LOLO(viu) = int	viu;
		HI(x, y) "Hello, welcome to americana %s\n", x, y
			printf(HI(variavel, var2)); = printf("Hello, welcome to americana %s\n", variavel, var2);

IFDEF IFnDEF LDEF
#ifdef TITI		//Caso tenha TITI no arquivo, tudo entre ifdef e endif sera utilizado
					#ifdef SEND
					void funcaodeenviar(bla bla bla)
					{
						a funcao;
					}
					#endif

#ifndef TUTU	//Caso nao tenha TUTU no arquivo, tudo entre ifdef e endif sera utilizado.
#lif TOTO		// lif significa else if. Funciona da mesma forma que os acima.


