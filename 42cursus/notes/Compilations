gcc commands:

gcc asd.c qwe.c zxc.c //compila todos os aruivos c em um exec.
-o execfile //colocado no fim da linha gcc cria o arquivo executavel com o nome de "execfile" por ex.

-c //compila o arquivo mas nao em executavel "file.o". O ".o" consegue se compilar com um executavel ja existente
//nao sendo assim necessario recompilar todos arquivos novamente.
//tambem compila arquivos sem main. Necessario para criar library.

-L. indica que a lib que quer usar esta na pasta atual.
-lstr //indica o nome da sua lib. Sua lib deve estar nomeada com "Lib" no inicio e str eh o nome dela.
ex.: "gcc prog.c -L. -libstr




libraries:

ar rc "nomedalib.a" "FUNCOES.O" "funcao2.o" //cria a lib com as fucoes indicadas.

ranlib "sualib.a" //cria um index das funcoes da su lib. Torna a compilacao mais rapida por ser mais facil encontrar a funcao desejada.


INCLUDE

#include //tem de colocar o # antes para mostrar que eh algo pre-processamento
#include <> //<> inclui arquivos do caminho do ambiente de sistema do stdio.h.
#include "" //"" inclui arquivos na pasta do proprio arquivo c
#includd